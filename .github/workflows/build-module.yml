name: 打包模块

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/build-module.yml"
      - ".gitattributes"
      - "build.ps1"
      - "module.prop"
      - "*.sh"
      - "config.conf.example"
      - "services.list.example"
      - "META-INF/**"
      - "system/**"
      - "tests/**"
      - "app/**"
      - "build.gradle"
      - "settings.gradle"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"
  pull_request:
    paths:
      - ".github/workflows/build-module.yml"
      - ".gitattributes"
      - "build.ps1"
      - "module.prop"
      - "*.sh"
      - "config.conf.example"
      - "services.list.example"
      - "META-INF/**"
      - "system/**"
      - "tests/**"
      - "app/**"
      - "build.gradle"
      - "settings.gradle"
      - "gradle.properties"
      - "gradle/**"
      - "gradlew"
      - "gradlew.bat"

permissions:
  contents: read

concurrency:
  group: module-build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package-module:
    name: 测试并打包 Root 模块
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 配置 JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: 配置 Android SDK
        uses: android-actions/setup-android@v3

      - name: 安装 Android SDK 36
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: 配置 Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: 设置测试脚本执行权限
        run: chmod +x gradlew tests/test-a11yctl.sh tests/test-install-manager.sh

      - name: 运行模块回归测试
        run: |
          ./tests/test-a11yctl.sh
          ./tests/test-install-manager.sh

      - name: 构建随模块安装的管理器
        run: ./gradlew --no-daemon :app:testDebugUnitTest :app:assembleDebug

      - name: 打包模块
        shell: pwsh
        run: ./build.ps1

      - name: 校验模块 ZIP
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob

          archives=(dist/magisk-accessibility-manager-*.zip)
          if [[ ${#archives[@]} -ne 1 ]]; then
            echo "Expected one module ZIP, found ${#archives[@]}" >&2
            exit 1
          fi
          archive="${archives[0]}"

          unzip -t "$archive"
          unzip -Z1 "$archive" | sort > "$RUNNER_TEMP/actual-files.txt"
          sort > "$RUNNER_TEMP/expected-files.txt" <<'EOF'
          META-INF/com/google/android/update-binary
          META-INF/com/google/android/updater-script
          action.sh
          config.conf.example
          customize.sh
          install-manager.sh
          manager.apk
          module.prop
          service.sh
          services.list.example
          system/bin/a11yctl
          uninstall.sh
          EOF
          diff -u "$RUNNER_TEMP/expected-files.txt" "$RUNNER_TEMP/actual-files.txt"

          while IFS= read -r entry; do
            unzip -p "$archive" "$entry" > "$RUNNER_TEMP/module-entry.txt"
            if grep -q $'\r' "$RUNNER_TEMP/module-entry.txt"; then
              echo "CRLF line ending found in $entry" >&2
              exit 1
            fi
          done <<'EOF'
          META-INF/com/google/android/update-binary
          META-INF/com/google/android/updater-script
          action.sh
          config.conf.example
          customize.sh
          install-manager.sh
          module.prop
          service.sh
          services.list.example
          system/bin/a11yctl
          uninstall.sh
          EOF

          cp "$archive" "$RUNNER_TEMP/first-build.zip"
          pwsh -File ./build.ps1
          cmp "$archive" "$RUNNER_TEMP/first-build.zip"
          sha256sum "$archive"

      - name: 上传模块 ZIP
        uses: actions/upload-artifact@v4
        with:
          name: accessibility-manager-module-${{ github.run_number }}
          path: dist/magisk-accessibility-manager-*.zip
          if-no-files-found: error
          retention-days: 14
